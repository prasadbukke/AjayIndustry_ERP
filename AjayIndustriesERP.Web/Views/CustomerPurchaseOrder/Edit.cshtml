@*
    ============================================================
    File: Edit.cshtml

    Purpose:
    Edits an existing Draft Customer Purchase Order.

    Responsibilities:
    - Own the Edit form.
    - Preserve Customer PO Id.
    - Render reusable Customer PO _Form.
    - Enable client-side validation.
    - Display validation errors through shared Toast.

    Important:
    Only Draft Customer Purchase Orders can reach this page.
    Customer PO Code remains system generated and read-only.
    ============================================================
*@

@model AjayIndustriesERP.Web.ViewModels.CustomerPurchaseOrder.CustomerPurchaseOrderFormViewModel

@{
    ViewData["Title"] = "Edit Customer PO";
}


<!-- =========================================================
     REGION 1 — PAGE HEADER
========================================================== -->

<div class="d-flex
            justify-content-between
            align-items-center
            flex-wrap
            gap-3
            mb-4">

    <div>

        <h2 class="fw-bold mb-1">
            Edit Customer Purchase Order
        </h2>

        <p class="text-muted mb-0">
            @Model.Code
        </p>

    </div>


    <a asp-action="Index"
       class="btn btn-outline-secondary">

        <i class="fa-solid fa-arrow-left me-1"></i>

        Back

    </a>

</div>


<!-- =========================================================
     REGION 2 — EDIT FORM
========================================================== -->

<form asp-action="Edit"
      asp-route-id="@Model.Id"
      method="post"
      id="customerPurchaseOrderForm">

    @Html.AntiForgeryToken()


    <input asp-for="Id"
           type="hidden" />

    <input asp-for="Status"
           type="hidden" />


    <partial name="_Form"
             model="Model" />


    <!-- =====================================================
         REGION 3 — FORM ACTIONS
    ====================================================== -->

    <div class="d-flex
                justify-content-end
                gap-2
                mb-4">

        <a asp-action="Index"
           class="btn btn-outline-secondary">

            Cancel

        </a>


        <button type="submit"
                class="btn btn-warning">

            <i class="fa-solid fa-floppy-disk me-1"></i>

            Update Customer PO

        </button>

    </div>

</form>


<!-- =========================================================
     REGION 4 — VALIDATION SCRIPTS
========================================================== -->
@section Scripts
{
    <partial name="_ValidationScriptsPartial" />

    <script>

        $(function () {

            $("#customerPurchaseOrderForm")
                .on(
                    "invalid-form.validate",
                    function (
                        event,
                        validator) {

                        const messages =
                            [
                                ...new Set(
                                    validator.errorList
                                        .map(
                                            x =>
                                                x.message
                                        )
                                )
                            ];


                        if (
                            window.showAppToast &&
                            messages.length > 0
                        ) {

                            window.showAppToast(
                                "error",
                                messages.join(
                                    " • "
                                )
                            );
                        }
                    }
                );

        });

    </script>
}